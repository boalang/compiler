package boa.kotlin.test

fun foo() {
    // Assignments
    x = 5
    x += 5
    x -= 5
    x *= 5
    x /= 5
    x %= 5

    // Arithmetic
    x - 5
    x + 5
    x * 5
    x / 5
    x % 5

    // Comparison
    x <= 5
    x >= 5
    x > 5
    x < 5
    x == 5
    x != 5

    // Logical
    x && y
    x || y

    // Bitwise
    x and y
    x or y
    x xor y
    x shl y
    x shr y
    x ushr y
}
