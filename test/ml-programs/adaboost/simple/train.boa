type T = enum {y = "true", n = "false"};
label: T;

p: Project = input;
ada : output adaboost("-s 75") of {int, T, int, T};

i := 0;
while(i < 100){
	label = T.y;
	if (i % 2 == 0)
		label = T.n;
	a: T = T.n;
	ada << {i + 7, a, i, label};
	i++;
}