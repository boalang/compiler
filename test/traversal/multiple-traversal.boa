m: output collection of int;

p: Project = input;

# traversal with return type
allnode_ids := traversal(node: CFGNode) : string {
	return string(node.id);
};

# traversal with no return type
dom_result := traversal(node: CFGNode) {
	j:= node.id;
};

m << 1;

