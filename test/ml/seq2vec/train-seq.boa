# ------- Seq2Vec - train - seq -------------
p: Project = input;
s2v: output seq2vec of queue of string;

q: queue of string;
offer(q, "a");
offer(q, "#");
offer(q, "b");
offer(q, "#");
offer(q, "c");
visit(p, visitor {
	before r : Revision -> {
		s2v << q;
	}
});