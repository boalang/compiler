type T = enum {y = "true", n = "false"};

p: Project = input;
predict: output collection of string;

prt: model of PART of {int, T, int, T};
prt = load(0, prt);

x1 := 1;
x2: T = T.n;
x3 := 2;

predict << classify(prt, {x1, x2, x3});