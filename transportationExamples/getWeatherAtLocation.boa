p: County = input;
counts: output sum[float][float][float][float] of int;
foreach(i : int; def(p.grid[i])){
	weatherRoot := getweather(p.grid[i]);
	foreach(j : int; def(weatherRoot.weather[j])){
	  counts[weatherRoot.weather[j].pcpn]
	  [weatherRoot.weather[j].roadtmpc]
	  [weatherRoot.weather[j].latlon.lat]
	  [weatherRoot.weather[j].latlon.lon]<<1;
	}
}
