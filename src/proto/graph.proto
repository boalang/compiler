package boa.types;

option optimize_for = SPEED;

message Traversal {
	enum TraversalDirection {
		BACKWARD = 1; 
		FORWARD = 2;
	}
	enum TraversalKind {
		DEEP_ITERATIVE = 3;
		SHALLOW_ITERATIVE = 4;  
		DFS = 6;
		POSTORDER = 7;
		WORKLIST = 8;
	}
}
